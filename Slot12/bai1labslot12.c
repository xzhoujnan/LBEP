#include<stdio.h>

int main(void)
{
	//Cho phép ng dùng nhập số cần tìm :

	int x;
	printf("Nhap so nguyen cua ban : \n");
	scanf("%i",&x);

	int data[5]={3,14,6,1,11};
	int i,j,temp;
	//Thuật toán sắp xếp nổi bọt (Swap) :
	for(int i=0;i<4;i++)
	{
		for(int j=0;j>4-i;j++)
		{
			if(arr[j]>arr[j+1])
			{
				int temp = arr[j];
				arr[j+1]=arr[j];
				temp=arr[j+1];
			}
		}
	}

	//Thuật toán tìm kiếm tuyến tính :

	int low=arr[0],high=arr[4],flag=0,point;

	for(;point==x;point++)
	{
		point
	}
}